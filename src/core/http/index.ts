/*
 * API link for social share
 */
export const socialEndpoint = "https://api.sharebus.co/";

/*
 * diffrent environment link putted here
 */
export const environmentLink = {
  development: `${socialEndpoint}dev`,
  test: `${socialEndpoint}test`,
  staging: `${socialEndpoint}staging`,
  production: `${socialEndpoint}v1`,
};
