export enum TRIP_TYPE {
  FIXED = "FIXED",
  NORMAL = "NORMAL",
  AIRPORT = "AIRPORT",
  VIP = "VIP",
  LAYOVER = "LAYOVER",
  ONEWAY = "oneway",
  RETURN = "return",
}

export enum ACTIONS {
  ADD = "add",
  REMOVE = "remove",
  DRAG_DROP = "drag_drop",
}
