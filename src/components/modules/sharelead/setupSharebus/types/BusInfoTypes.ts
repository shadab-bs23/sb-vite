// This file contains types for the BusInfo API responses
// Add specific types as needed by the project

export interface BusInfoApi {
  id?: number;
  total: number;
  currency?: string;
  // Add other properties as needed
}
